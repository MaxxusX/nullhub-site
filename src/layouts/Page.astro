---
interface Props {
	path: string;
	title: string;
}

const { path, title } = Astro.props;

// components
import Head from "../components/Head.astro";

// images
import bgImage from "../assets/reception_blur.webp";

// stylesheets
import "../styles/main.css";
const styleURL = new URL("../styles/main.css", import.meta.url).href;
---

<!doctype html>
<html lang="en">
	<Head path={path} title={title}>
		<Fragment slot="preload">
			<link
				rel="preload"
				href={bgImage.src}
				as="image"
				fetchpriority="high"
				referrerpolicy="no-referrer"
			/>
			<link
				rel="preload"
				href={styleURL}
				as="style"
				fetchpriority="high"
				referrerpolicy="no-referrer"
			/>
			<link
				rel="preload"
				href="/fonts/Hubot-Sans.woff2"
				as="font"
				fetchpriority="low"
				referrerpolicy="no-referrer"
			/>
			<link
				rel="preload"
				href="/fonts/Mona-Sans.woff2"
				as="font"
				fetchpriority="low"
				referrerpolicy="no-referrer"
			/>
		</Fragment>
	</Head>
	<body style={`background-image: url('${bgImage.src}');`}>
		<main>
			<slot />
		</main>
	</body>
</html>
